# Copy this file to `.env` and fill in the values.
# Never commit your real `.env` file.

# --- Contact form (server-side only) -------------------------------------
# API key from https://resend.com used to deliver contact-form messages to
# youstenasalah123@gmail.com. Without it, the form returns a friendly error.
RESEND_API_KEY=re_your_resend_api_key_here

# Optional: port for the local dev server (defaults to 8080)
# PORT=8080
